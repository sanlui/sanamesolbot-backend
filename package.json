{
  "name": "sanamesolbot-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"no tests\" && exit 0",
    "check": "node --check server.js && node --check poller.js && node --check storage.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.19.2"
  },
  "engines": {
    "node": "22.x"
  }
}
